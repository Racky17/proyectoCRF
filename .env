# Database configuration for proyectoCRF (used by docker-compose and Spring Boot)
# Edit these values as needed. The Spring Boot app reads MYSQL_URL, MYSQL_USER, MYSQL_PASS

# MySQL image init variables
MYSQL_DATABASE=ingsof
# Set a root password (non-empty). Change to something secure for production.
MYSQL_ROOT_PASSWORD=admin123
MYSQL_ALLOW_EMPTY_PASSWORD=no


# Application DB connection (must start with jdbc://)
MYSQL_URL=jdbc:mysql://db:3306/ingsof
# Use the dedicated app user for the application
